"use client";

import LiveTestShell from "./LiveTestShell";

/** Live-test dashboard with persistent guide rail (avatar + chat). */
export default function LiveGuideStage() {
  return <LiveTestShell />;
}
